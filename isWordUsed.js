const dbAPI = require("./dbAPI");

module.exports = function isWordUsed(word) {
    // query database with findone with id: word
    
    // if found, return true
    // else, return false
    return false;
};